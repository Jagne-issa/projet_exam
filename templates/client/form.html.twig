{% extends 'base.html.twig' %}

{% block main %}
<div class="container mx-auto p-4">
    <div class="bg-white shadow-md rounded-lg">
        <div class="bg-gray-200 p-4 rounded-t-lg">
            <h2 class="text-lg font-semibold">Enregistrement d'un client</h2>
        </div>
        <div class="p-4">
            {{ form(formClient) }}
        </div>
    </div>
</div>
{% endblock %}
