<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="h-screen md:flex">
        <div class="relative overflow-hidden md:flex w-1/2 bg-gradient-to-tr from-blue-800 to-purple-700 justify-around items-center hidden">
            <div>
                <h1 class="text-white font-bold text-4xl font-sans">Votre Boutique en Ligne</h1>
                <p class="text-white mt-1">Découvrez une expérience d'achat unique pour des produits artisanaux.</p>
                <button type="submit" class="block w-28 bg-white text-indigo-800 mt-4 py-2 rounded-2xl font-bold mb-2">Read More</button>
            </div>
            <div class="absolute -bottom-32 -left-40 w-80 h-80 border-4 rounded-full border-opacity-30 border-t-8"></div>
            <div class="absolute -bottom-40 -left-20 w-80 h-80 border-4 rounded-full border-opacity-30 border-t-8"></div>
            <div class="absolute -top-40 -right-0 w-80 h-80 border-4 rounded-full border-opacity-30 border-t-8"></div>
            <div class="absolute -top-20 -right-20 w-80 h-80 border-4 rounded-full border-opacity-30 border-t-8"></div>
        </div>
        <div class="flex md:w-1/2 justify-center py-10 items-center bg-white">
            <form class="bg-white w-80" method="POST" action="{{ path('app_login') }}">
                <h1 class="text-gray-800 font-bold text-2xl mb-1">Connexion</h1>
                <p class="text-sm font-normal text-gray-600 mb-7">Entrez vos informations pour vous connecter.</p>
            
                {% if error %}
                    <div class="text-red-500 text-sm mb-4">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}
            
                <div class="flex items-center border-2 py-2 px-3 rounded-2xl mb-4">
                    <input class="pl-2 outline-none border-none w-full" type="text" name="username" id="username" placeholder="Nom d'utilisateur ou Email" required value="{{ last_username }}" aria-label="Nom d'utilisateur ou Email" />
                </div>
            
                <div class="flex items-center border-2 py-2 px-3 rounded-2xl mb-4">
                    <input class="pl-2 outline-none border-none w-full" type="password" name="_password" id="password" placeholder="Mot de passe" required aria-label="Mot de passe" />
                </div>
            
                <button type="submit" class="block w-full bg-indigo-600 mt-4 py-2 rounded-2xl text-white font-semibold mb-2">Se connecter</button>
                
                <a href="/auth/google" class="block w-full text-center border-2 border-gray-300 text-gray-600 mt-2 py-2 rounded-2xl hover:bg-gray-100">
                    Se connecter avec Google
                </a>
            
                <span class="text-sm ml-2 hover:text-blue-500 cursor-pointer">
                    Pas encore de compte? <a href="/register" class="text-blue-500">Inscrivez-vous</a>
                </span>
            </form>
        </div>
    </div>
</body>
</html>
